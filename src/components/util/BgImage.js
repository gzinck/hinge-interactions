import styled from "@emotion/styled";

const BgImage = styled.img`
  width: 100vw;
  height: 100vh;
  position: absolute;
  user-select: none;
  pointer-events: none;
  left: 0;
  top: 0;
`;

export default BgImage;
